# 测试用例生成任务

请根据以下图谱节点信息，生成自动化测试用例。

## 节点信息
- 节点类型: {nodeType}
- 节点名称: {nodeName}
- 节点描述: {nodeDescription}
- 相关关系:
{relations}

## 生成要求
请针对这个节点生成合适的自动化测试用例：
1. 正常路径测试：正常输入应该得到正常输出
2. 异常路径测试：非法输入应该正确处理
3. 边界条件测试：边界值和特殊情况
4. 如果涉及数据库操作，需要包含数据库断言

## 输出格式
请以严格 JSON 格式输出：
```json
{
  "testCases": [
    {
      "caseName": "测试用例名称",
      "caseType": "NORMAL|EXCEPTION|BOUNDARY",
      "description": "测试描述",
      "request": {
        "method": "HTTP_METHOD",
        "url": "endpoint/path",
        "body": "请求体(JSON 格式)",
        "headers": {}
      },
      "expected": {
        "statusCode": 200,
        "responseContains": "期待响应包含的关键词",
        "dbAssertions": [
          { "table": "表名", "condition": "查询条件", "expectedCount": 1 }
        ]
      }
    }
  ]
}
```

只输出 JSON，不要其他解释内容。
