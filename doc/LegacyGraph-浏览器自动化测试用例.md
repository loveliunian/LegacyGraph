# LegacyGraph 浏览器自动化测试用例

> 适用工具：agent-browser（浏览器自动化测试）
> 测试对象：LegacyGraph AI 知识图谱分析平台前端
> 测试环境：开发环境（默认 http://localhost:5173）

---

## 目录

- [一、测试环境配置](#一测试环境配置)
- [二、测试账号](#二测试账号)
- [三、冒烟测试（Smoke Test）](#三冒烟测试smoke-test)
- [四、登录认证模块](#四登录认证模块)
- [五、仪表盘模块](#五仪表盘模块)
- [六、项目管理模块](#六项目管理模块)
- [七、数据源管理模块](#七数据源管理模块)
- [八、扫描管理模块](#八扫描管理模块)
- [九、图谱可视化模块](#九图谱可视化模块)
- [十、审核管理模块](#十审核管理模块)
- [十一、事实与证据模块](#十一事实与证据模块)
- [十二、测试用例管理模块](#十二测试用例管理模块)
- [十三、QA 问答模块](#十三qa-问答模块)
- [十四、Agent Hub 模块](#十四agent-hub-模块)
- [十五、迁移风险模块](#十五迁移风险模块)
- [十六、系统管理模块](#十六系统管理模块)
- [十七、异常与边界测试](#十七异常与边界测试)
- [十八、性能与兼容性测试](#十八性能与兼容性测试)

---

## 一、测试环境配置

### 1.1 环境准备

| 配置项 | 说明 |
|--------|------|
| 前端地址 | http://localhost:5173 |
| 后端地址 | http://localhost:8080/api |
| 浏览器 | Chrome / Chromium（最新版） |
| 视口大小 | 1920 x 1080（推荐桌面端） |
| 网络状态 | 正常网络（非弱网） |

### 1.2 前置条件

1. 后端服务已启动并正常运行
2. 前端开发服务已启动
3. 数据库（PostgreSQL + Neo4j + Redis）已就绪
4. 测试账号已创建
5. 至少存在一个测试项目，且已完成至少一次扫描

### 1.3 agent-browser 使用说明

- 使用 `navigate` 指令访问页面
- 使用 `click` 指令点击元素
- 使用 `type` 指令输入文本
- 使用 `snapshot` 获取页面结构
- 使用 `screenshot` 截图保存证据
- 每个测试用例执行完毕后，记录通过/失败状态

---

## 二、测试账号

| 角色 | 用户名 | 密码 | 权限说明 |
|------|--------|------|----------|
| 管理员 | admin | admin123 | 全部权限，含系统管理 |
| 普通用户 | testuser | test123 | 项目访问、扫描、查看等基础权限 |

---

## 三、冒烟测试（Smoke Test）

### TC-SMOKE-001：应用启动与首页加载

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SMOKE-001 |
| **测试场景** | 验证应用能否正常启动并加载首页 |
| **优先级** | P0 |
| **前置条件** | 前后端服务已启动 |
| **测试步骤** | 1. 访问 http://localhost:5173<br>2. 等待页面加载完成 |
| **预期结果** | 1. 页面正常加载，无白屏<br>2. 自动重定向到登录页（未登录状态）<br>3. 浏览器控制台无严重 JS 错误 |
| **验证方式** | 截图 + 控制台错误检查 |

### TC-SMOKE-002：登录后进入主应用

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SMOKE-002 |
| **测试场景** | 验证登录成功后能否进入主应用界面 |
| **优先级** | P0 |
| **前置条件** | 测试账号可用 |
| **测试步骤** | 1. 访问登录页<br>2. 输入 admin / admin123<br>3. 点击登录按钮<br>4. 等待跳转完成 |
| **预期结果** | 1. 登录成功，显示「登录成功」提示<br>2. URL 跳转到 /dashboard 或 /projects<br>3. 左侧导航菜单可见<br>4. 顶部用户头像/名称显示 |
| **验证方式** | URL 检查 + 元素可见性检查 |

### TC-SMOKE-003：主要菜单导航可用性

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SMOKE-003 |
| **测试场景** | 验证左侧主要菜单项能否正常点击和跳转 |
| **优先级** | P0 |
| **前置条件** | 已登录，存在至少一个项目 |
| **测试步骤** | 1. 依次点击左侧菜单：仪表盘、项目管理、数据源、扫描管理、图谱、事实、测试、迁移风险<br>2. 每次点击后等待页面加载 |
| **预期结果** | 1. 每个菜单点击后页面正确跳转<br>2. URL 与菜单项对应<br>3. 页面无 404 错误<br>4. 页面主要内容区域正常显示 |
| **验证方式** | URL 断言 + 无错误页面 |

---

## 四、登录认证模块

### TC-AUTH-001：登录页面元素完整性

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AUTH-001 |
| **测试场景** | 验证登录页面 UI 元素是否完整 |
| **优先级** | P1 |
| **前置条件** | 访问 /login 页面 |
| **测试步骤** | 1. 访问 /login<br>2. 检查页面元素 |
| **预期结果** | 1. 显示 Logo 或标题「LegacyGraph」<br>2. 显示副标题「AI 图谱理解平台」<br>3. 用户名输入框可见（placeholder：请输入用户名）<br>4. 密码输入框可见（placeholder：请输入密码）<br>5. 「记住我」复选框可见<br>6. 「登录」按钮可见且默认可用<br>7. 底部版权信息可见 |
| **验证方式** | 元素可见性检查 |

### TC-AUTH-002：正确账号登录成功

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AUTH-002 |
| **测试场景** | 验证使用正确账号密码登录成功 |
| **优先级** | P0 |
| **前置条件** | 账号 admin / admin123 存在 |
| **测试步骤** | 1. 在用户名输入框输入「admin」<br>2. 在密码输入框输入「admin123」<br>3. 点击「登录」按钮<br>4. 等待页面跳转 |
| **预期结果** | 1. 显示成功提示「登录成功」<br>2. 跳转到仪表盘或项目列表页<br>3. 左侧菜单可见<br>4. 顶部显示当前用户名 |
| **验证方式** | URL 变化 + 成功提示 |

### TC-AUTH-003：错误密码登录失败

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AUTH-003 |
| **测试场景** | 验证使用错误密码登录失败 |
| **优先级** | P1 |
| **前置条件** | 访问登录页 |
| **测试步骤** | 1. 输入用户名「admin」<br>2. 输入错误密码「wrongpass」<br>3. 点击「登录」按钮 |
| **预期结果** | 1. 显示错误提示消息（红色）<br>2. 停留在登录页<br>3. 密码框被清空或保留输入 |
| **验证方式** | 错误消息可见 + URL 不变 |

### TC-AUTH-004：空表单提交验证

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AUTH-004 |
| **测试场景** | 验证空表单提交时的前端校验 |
| **优先级** | P2 |
| **前置条件** | 访问登录页 |
| **测试步骤** | 1. 用户名和密码均留空<br>2. 点击「登录」按钮或按回车 |
| **预期结果** | 1. 用户名下方显示「请输入用户名」<br>2. 密码下方显示「请输入密码」<br>3. 不发起登录请求 |
| **验证方式** | 校验错误文案可见 |

### TC-AUTH-005：记住我功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AUTH-005 |
| **测试场景** | 验证记住我勾选状态保存 |
| **优先级** | P2 |
| **前置条件** | 访问登录页 |
| **测试步骤** | 1. 勾选「记住我」复选框<br>2. 输入正确账号密码登录<br>3. 登出后再次访问登录页 |
| **预期结果** | 1. 用户名被自动填充（如果实现了记住我）<br>2. 「记住我」复选框保持勾选状态 |
| **验证方式** | 检查输入框值 |

### TC-AUTH-006：回车提交登录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AUTH-006 |
| **测试场景** | 验证在密码框按回车键可提交登录 |
| **优先级** | P2 |
| **前置条件** | 访问登录页 |
| **测试步骤** | 1. 输入正确用户名和密码<br>2. 在密码输入框按 Enter 键 |
| **预期结果** | 1. 触发表单提交<br>2. 登录成功（与点击按钮行为一致） |
| **验证方式** | 登录成功跳转 |

### TC-AUTH-007：登出功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AUTH-007 |
| **测试场景** | 验证用户登出功能 |
| **优先级** | P1 |
| **前置条件** | 已登录状态 |
| **测试步骤** | 1. 点击右上角用户头像/名称<br>2. 在下拉菜单中点击「退出登录」<br>3. 确认登出（如有确认弹窗） |
| **预期结果** | 1. 跳转到登录页<br>2. 登录页显示干净（无用户信息）<br>3. 访问受保护页面会被重定向回登录页 |
| **验证方式** | URL 跳转 + 受保护页重定向 |

### TC-AUTH-008：未登录访问受保护页面

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AUTH-008 |
| **测试场景** | 验证未登录用户访问受保护页面自动跳转登录 |
| **优先级** | P1 |
| **前置条件** | 浏览器处于未登录状态（清除本地存储） |
| **测试步骤** | 1. 直接访问 /dashboard<br>2. 观察页面跳转 |
| **预期结果** | 1. 自动重定向到 /login<br>2. URL 包含 redirect 参数（如有实现） |
| **验证方式** | URL 重定向检查 |

### TC-AUTH-009：Token 过期自动登出

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AUTH-009 |
| **测试场景** | 验证 token 过期后自动跳转登录页 |
| **优先级** | P2 |
| **前置条件** | 已登录 |
| **测试步骤** | 1. 通过开发者工具将 localStorage 中的 token 过期时间改为过去时间<br>2. 刷新页面或点击任意菜单 |
| **预期结果** | 1. 自动清除登录态<br>2. 跳转到登录页<br>3. 显示登录过期提示（如有） |
| **验证方式** | 重定向到登录页 |

---

## 五、仪表盘模块

### TC-DASH-001：仪表盘页面加载

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-DASH-001 |
| **测试场景** | 验证仪表盘页面正常加载 |
| **优先级** | P1 |
| **前置条件** | 已登录，存在当前项目 |
| **测试步骤** | 1. 登录后进入仪表盘<br>2. 等待所有数据加载完成 |
| **预期结果** | 1. 页面标题/面包屑显示「仪表盘」<br>2. 项目总体统计卡片可见<br>3. 节点类型分布卡片可见<br>4. 置信度分布图可见<br>5. 数据源状态卡片可见<br>6. 当前项目信息卡片可见<br>7. 待审核列表卡片可见<br>8. 快速操作卡片可见 |
| **验证方式** | 各卡片元素可见性检查 |

### TC-DASH-002：统计数据展示正确性

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-DASH-002 |
| **测试场景** | 验证仪表盘统计数据正常显示 |
| **优先级** | P1 |
| **前置条件** | 项目已完成扫描，有图谱数据 |
| **测试步骤** | 1. 进入仪表盘<br>2. 检查各项统计数字 |
| **预期结果** | 1. 图谱节点数为数字且 >= 0<br>2. 已确认节点数 <= 总节点数<br>3. 关系连接数为数字且 >= 0<br>4. 已确认关系数 <= 总关系数<br>5. 平均置信度在 0-100% 之间<br>6. 节点确认率在 0-100% 之间<br>7. 关系确认率在 0-100% 之间 |
| **验证方式** | 数值范围断言 |

### TC-DASH-003：节点类型分布展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-DASH-003 |
| **测试场景** | 验证节点类型分布列表正常展示 |
| **优先级** | P2 |
| **前置条件** | 项目有图谱数据 |
| **测试步骤** | 1. 进入仪表盘<br>2. 滚动到节点类型分布区域 |
| **预期结果** | 1. 显示至少 3 种以上节点类型<br>2. 每种类型有进度条（已确认/总数）<br>3. 进度条颜色与置信度对应<br>4. 显示置信度百分比 |
| **验证方式** | 类型行数 > 0 + 进度条可见 |

### TC-DASH-004：置信度分布柱状图

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-DASH-004 |
| **测试场景** | 验证置信度分布柱状图正常渲染 |
| **优先级** | P2 |
| **前置条件** | 项目有图谱数据 |
| **测试步骤** | 1. 进入仪表盘<br>2. 找到置信度分布区域 |
| **预期结果** | 1. 显示 5 个置信区间的柱形（0-20, 20-40, 40-60, 60-80, 80-100）<br>2. 柱形有高度（非全 0 时）<br>3. 柱形颜色从红色到绿色渐变<br>4. 每个柱形上方显示数量 |
| **验证方式** | 柱形元素数量 = 5 |

### TC-DASH-005：数据源状态统计

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-DASH-005 |
| **测试场景** | 验证数据源状态卡片正确显示 |
| **优先级** | P2 |
| **前置条件** | 项目配置了数据源 |
| **测试步骤** | 1. 进入仪表盘<br>2. 查看数据源状态卡片 |
| **预期结果** | 1. 显示代码仓库数量<br>2. 显示数据库连接数量<br>3. 显示文档资料数量<br>4. 每个数据源有对应图标 |
| **验证方式** | 三项数据均可见 |

### TC-DASH-006：待审核列表

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-DASH-006 |
| **测试场景** | 验证待审核列表展示与跳转 |
| **优先级** | P1 |
| **前置条件** | 有待审核项 |
| **测试步骤** | 1. 进入仪表盘<br>2. 查看待审核卡片<br>3. 点击「去审核 →」链接 |
| **预期结果** | 1. 待审核数量标签可见<br>2. 列表展示最多 5 条待审核项<br>3. 每项显示节点名称和置信度<br>4. 置信度标签颜色正确（低/中/高）<br>5. 点击「去审核」跳转到审核列表页 |
| **验证方式** | 列表项可见 + 跳转正确 |

### TC-DASH-007：快速操作按钮

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-DASH-007 |
| **测试场景** | 验证快速操作按钮功能 |
| **优先级** | P2 |
| **前置条件** | 已选择项目 |
| **测试步骤** | 1. 进入仪表盘<br>2. 依次点击快速操作按钮：新扫描、迁移报告、合并节点、执行测试 |
| **预期结果** | 1. 「新扫描」跳转到扫描创建页<br>2. 「迁移报告」跳转到报告页或触发生成<br>3. 「合并节点」跳转到统一图谱页<br>4. 「执行测试」跳转到测试用例页 |
| **验证方式** | 每次点击 URL 正确变化 |

### TC-DASH-008：迁移就绪度评估

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-DASH-008 |
| **测试场景** | 验证迁移就绪度评估展示 |
| **优先级** | P2 |
| **前置条件** | 项目有迁移报告数据 |
| **测试步骤** | 1. 进入仪表盘<br>2. 滚动到迁移就绪度评估区域 |
| **预期结果** | 1. 显示总体评分（0-100 分）<br>2. 显示评分等级描述（就绪/基本就绪等）<br>3. 显示分项评分（架构理解、业务知识覆盖、整体置信度）<br>4. 显示风险项（如有）<br>5. 显示 AI 建议列表（如有）<br>6. 刷新按钮可点击 |
| **验证方式** | 评分数值在 0-100 之间 |

---

## 六、项目管理模块

### TC-PROJ-001：项目列表页面加载

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PROJ-001 |
| **测试场景** | 验证项目列表页面正常显示 |
| **优先级** | P1 |
| **前置条件** | 已登录 |
| **测试步骤** | 1. 点击左侧「项目管理」→「项目列表」<br>2. 等待页面加载 |
| **预期结果** | 1. 页面标题显示「项目列表」<br>2. 顶部有搜索框和「新建项目」按钮<br>3. 数据表格可见<br>4. 分页器可见（如数据多于一页） |
| **验证方式** | 表格 + 按钮可见 |

### TC-PROJ-002：项目列表搜索

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PROJ-002 |
| **测试场景** | 验证项目列表关键词搜索功能 |
| **优先级** | P1 |
| **前置条件** | 项目列表有数据 |
| **测试步骤** | 1. 进入项目列表页<br>2. 在搜索框输入项目名称关键词（如「测试」）<br>3. 按回车或点击搜索按钮 |
| **预期结果** | 1. 表格数据刷新，只显示匹配的项目<br>2. 搜索关键词高亮（如有）<br>3. 清空搜索后恢复全部数据 |
| **验证方式** | 搜索前后表格行数变化 |

### TC-PROJ-003：新建项目（正向）

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PROJ-003 |
| **测试场景** | 验证新建项目功能正常 |
| **优先级** | P1 |
| **前置条件** | 已登录 |
| **测试步骤** | 1. 点击「新建项目」按钮<br>2. 填写项目名称：自动化测试项目<br>3. 填写项目编码：auto-test-001<br>4. 填写负责人：tester<br>5. 填写描述：这是一个自动化测试项目<br>6. 点击「确认」或「创建」按钮 |
| **预期结果** | 1. 弹出成功提示<br>2. 对话框关闭<br>3. 项目列表中出现新项目<br>4. 新项目排在第一位（按创建时间倒序） |
| **验证方式** | 成功提示 + 列表新增一行 |

### TC-PROJ-004：新建项目表单校验

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PROJ-004 |
| **测试场景** | 验证新建项目表单的必填项校验 |
| **优先级** | P2 |
| **前置条件** | 打开新建项目对话框 |
| **测试步骤** | 1. 项目名称留空，提交表单<br>2. 观察校验提示<br>3. 填写项目名称，项目编码留空，提交<br>4. 观察校验提示 |
| **预期结果** | 1. 项目名称为空时显示「请输入项目名称」<br>2. 项目编码为空时显示「请输入项目编码」<br>3. 表单不提交，对话框不关闭 |
| **验证方式** | 校验错误文案可见 |

### TC-PROJ-005：项目详情页访问

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PROJ-005 |
| **测试场景** | 验证点击项目可进入详情页 |
| **优先级** | P1 |
| **前置条件** | 项目列表有数据 |
| **测试步骤** | 1. 在项目列表中点击某一行的项目名称<br>2. 或点击操作列的「详情」按钮 |
| **预期结果** | 1. 跳转到项目详情页（URL 包含项目 ID）<br>2. 页面显示项目名称作为标题<br>3. 左侧显示项目内菜单（概览、代码仓库、数据库、文档等）<br>4. 默认显示项目概览页面 |
| **验证方式** | URL 包含项目 ID + 详情页元素可见 |

### TC-PROJ-006：项目编辑功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PROJ-006 |
| **测试场景** | 验证项目信息编辑功能 |
| **优先级** | P2 |
| **前置条件** | 存在可编辑的项目 |
| **测试步骤** | 1. 进入项目列表<br>2. 找到某项目，点击「编辑」<br>3. 修改项目描述<br>4. 点击保存 |
| **预期结果** | 1. 保存成功提示<br>2. 列表中该项目信息更新<br>3. 详情页信息同步更新 |
| **验证方式** | 成功提示 + 数据更新 |

### TC-PROJ-007：项目删除功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PROJ-007 |
| **测试场景** | 验证项目删除功能（含二次确认） |
| **优先级** | P2 |
| **前置条件** | 存在一个专门用于删除测试的项目 |
| **测试步骤** | 1. 在项目列表找到测试项目<br>2. 点击「删除」按钮<br>3. 在确认弹窗中点击「取消」<br>4. 再次点击「删除」<br>5. 在确认弹窗中点击「确认」 |
| **预期结果** | 1. 第一次取消后，项目仍在列表中<br>2. 第二次确认后，弹出成功提示<br>3. 项目从列表中消失 |
| **验证方式** | 取消保留 + 确认删除 |

### TC-PROJ-008：项目概览页

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PROJ-008 |
| **测试场景** | 验证项目概览页内容展示 |
| **优先级** | P2 |
| **前置条件** | 进入项目详情页 |
| **测试步骤** | 1. 在项目详情页点击「概览」<br>2. 检查页面内容 |
| **预期结果** | 1. 显示项目基本信息（名称、编码、负责人、创建时间）<br>2. 显示扫描统计（扫描次数、最新扫描时间）<br>3. 显示图谱数据摘要<br>4. 显示快速操作入口 |
| **验证方式** | 各信息区块可见 |

---

## 七、数据源管理模块

### TC-SRC-001：代码仓库列表页

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SRC-001 |
| **测试场景** | 验证代码仓库列表页面 |
| **优先级** | P1 |
| **前置条件** | 进入项目详情页 |
| **测试步骤** | 1. 在项目详情左侧菜单点击「代码仓库」<br>2. 等待页面加载 |
| **预期结果** | 1. 页面标题显示「代码仓库」<br>2. 有「添加代码仓库」按钮<br>3. 表格列包含：仓库名称、仓库地址、分支、状态、操作<br>4. 空状态提示友好（无数据时） |
| **验证方式** | 表格 + 添加按钮可见 |

### TC-SRC-002：添加代码仓库

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SRC-002 |
| **测试场景** | 验证添加代码仓库功能 |
| **优先级** | P1 |
| **前置条件** | 在代码仓库列表页 |
| **测试步骤** | 1. 点击「添加代码仓库」<br>2. 填写仓库名称：test-repo<br>3. 填写仓库 URL（本地路径或 Git 地址）<br>4. 填写分支：main<br>5. 点击「确认」 |
| **预期结果** | 1. 成功提示消息<br>2. 列表中新增一行记录<br>3. 状态显示「待扫描」或「已添加」 |
| **验证方式** | 列表新增记录 |

### TC-SRC-003：数据库连接列表

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SRC-003 |
| **测试场景** | 验证数据库连接列表页面 |
| **优先级** | P1 |
| **前置条件** | 进入项目详情页 |
| **测试步骤** | 1. 点击左侧菜单「数据库」<br>2. 等待页面加载 |
| **预期结果** | 1. 显示「数据库连接」标题<br>2. 有「添加数据库」按钮<br>3. 表格列：连接名称、数据库类型、主机、端口、状态、操作<br>4. 有「测试连接」操作（如有） |
| **验证方式** | 表格和按钮可见 |

### TC-SRC-004：添加数据库连接

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SRC-004 |
| **测试场景** | 验证添加数据库连接功能 |
| **优先级** | P1 |
| **前置条件** | 在数据库列表页 |
| **测试步骤** | 1. 点击「添加数据库」<br>2. 选择数据库类型：PostgreSQL<br>3. 填写连接名称：test-db<br>4. 填写主机：localhost<br>5. 填写端口：5432<br>6. 填写数据库名：testdb<br>7. 填写用户名：postgres<br>8. 填写密码：password<br>9. 点击「测试连接」（如有）<br>10. 点击「确认」保存 |
| **预期结果** | 1. 测试连接显示成功（如数据库可达）<br>2. 保存成功提示<br>3. 列表新增数据库连接 |
| **验证方式** | 成功提示 + 列表新增 |

### TC-SRC-005：文档资料列表

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SRC-005 |
| **测试场景** | 验证文档资料列表页面 |
| **优先级** | P1 |
| **前置条件** | 进入项目详情页 |
| **测试步骤** | 1. 点击左侧菜单「文档」<br>2. 等待页面加载 |
| **预期结果** | 1. 显示「文档资料」标题<br>2. 有「上传文档」按钮或拖拽上传区域<br>3. 表格列：文档名称、类型、大小、上传时间、状态、操作 |
| **验证方式** | 上传区域 + 列表可见 |

### TC-SRC-006：文档上传功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SRC-006 |
| **测试场景** | 验证文档上传功能 |
| **优先级** | P1 |
| **前置条件** | 在文档列表页 |
| **测试步骤** | 1. 点击「上传文档」或将文件拖入上传区域<br>2. 选择一个 .md 或 .docx 文件<br>3. 等待上传完成 |
| **预期结果** | 1. 上传进度条可见<br>2. 上传完成后显示成功提示<br>3. 文档列表中出现新文件<br>4. 文件状态为「已上传」或「处理中」 |
| **验证方式** | 列表新增文件 + 成功提示 |

---

## 八、扫描管理模块

### TC-SCAN-001：扫描版本列表页

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SCAN-001 |
| **测试场景** | 验证扫描版本列表页面 |
| **优先级** | P1 |
| **前置条件** | 进入项目详情页 |
| **测试步骤** | 1. 点击左侧菜单「扫描版本」<br>2. 等待页面加载 |
| **预期结果** | 1. 显示「扫描版本」标题<br>2. 有「新建扫描」按钮<br>3. 表格列：版本号、状态、扫描类型、开始时间、耗时、操作<br>4. 状态标签颜色正确（成功-绿色、运行中-蓝色、失败-红色） |
| **验证方式** | 表格 + 新建按钮可见 |

### TC-SCAN-002：新建扫描向导 - 步骤1 选择范围

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SCAN-002 |
| **测试场景** | 验证新建扫描向导第一步：选择扫描范围 |
| **优先级** | P1 |
| **前置条件** | 项目已配置至少一个数据源 |
| **测试步骤** | 1. 点击「新建扫描」按钮<br>2. 进入步骤1（选择扫描范围） |
| **预期结果** | 1. 步骤条显示，当前在第 1 步<br>2. 显示可选的数据源列表（代码仓库、数据库、文档）<br>3. 每个数据源前有复选框<br>4. 「下一步」按钮可见（可能默认禁用） |
| **验证方式** | 步骤条 + 数据源列表可见 |

### TC-SCAN-003：新建扫描向导 - 步骤2 选择类型

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SCAN-003 |
| **测试场景** | 验证新建扫描向导第二步：选择扫描类型 |
| **优先级** | P1 |
| **前置条件** | 完成步骤1，已选择数据源 |
| **测试步骤** | 1. 在步骤1勾选至少一个数据源<br>2. 点击「下一步」<br>3. 进入步骤2 |
| **预期结果** | 1. 步骤条前进到第 2 步<br>2. 显示扫描类型选项（代码图谱、功能图谱、业务图谱等）<br>3. 可多选扫描类型<br>4. 有「上一步」和「下一步」按钮 |
| **验证方式** | 步骤条高亮第2步 + 扫描类型选项可见 |

### TC-SCAN-004：新建扫描向导 - 步骤3 配置参数

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SCAN-004 |
| **测试场景** | 验证新建扫描向导第三步：配置扫描参数 |
| **优先级** | P2 |
| **前置条件** | 完成步骤2 |
| **测试步骤** | 1. 在步骤2选择扫描类型<br>2. 点击「下一步」<br>3. 进入步骤3 |
| **预期结果** | 1. 步骤条前进到第 3 步<br>2. 显示任务名称输入框（有默认值）<br>3. 显示高级配置项（如 chunk size、并行数等）<br>4. 有「上一步」和「开始扫描」按钮 |
| **验证方式** | 配置表单可见 |

### TC-SCAN-005：启动扫描任务

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SCAN-005 |
| **测试场景** | 验证点击开始扫描后任务启动 |
| **优先级** | P0 |
| **前置条件** | 完成扫描配置 |
| **测试步骤** | 1. 在扫描向导最后一步点击「开始扫描」<br>2. 返回扫描版本列表 |
| **预期结果** | 1. 显示「扫描已启动」成功提示<br>2. 扫描版本列表新增一条记录<br>3. 状态为「运行中」或 PENDING<br>4. 有进度条或百分比显示 |
| **验证方式** | 新增记录 + 运行中状态 |

### TC-SCAN-006：扫描进度实时显示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SCAN-006 |
| **测试场景** | 验证扫描进度实时更新 |
| **优先级** | P1 |
| **前置条件** | 有正在运行的扫描任务 |
| **测试步骤** | 1. 在扫描版本列表观察运行中的任务<br>2. 停留页面 10-30 秒<br>3. 观察进度变化 |
| **预期结果** | 1. 进度百分比持续增长<br>2. 显示当前执行阶段（如 ADAPTER_SCAN、AI_ORCHESTRATION）<br>3. 扫描完成后状态变为「成功」<br>4. 显示总耗时 |
| **验证方式** | 进度数值递增 + 最终 SUCCESS 状态 |

### TC-SCAN-007：扫描详情查看

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SCAN-007 |
| **测试场景** | 验证点击扫描版本可查看详情 |
| **优先级** | P2 |
| **前置条件** | 有已完成的扫描版本 |
| **测试步骤** | 1. 在扫描版本列表点击某版本号或「详情」按钮<br>2. 查看详情页 |
| **预期结果** | 1. 显示扫描基本信息（版本、状态、时间、耗时）<br>2. 显示各阶段耗时统计<br>3. 显示扫描结果摘要（节点数、边数等）<br>4. 有「查看图谱」按钮 |
| **验证方式** | 详情页各信息区块可见 |

### TC-SCAN-008：扫描任务取消

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SCAN-008 |
| **测试场景** | 验证运行中的扫描可以取消 |
| **优先级** | P2 |
| **前置条件** | 有正在运行的扫描任务 |
| **测试步骤** | 1. 找到运行中的扫描任务<br>2. 点击「取消」按钮<br>3. 确认取消 |
| **预期结果** | 1. 确认弹窗出现<br>2. 确认后状态变为「已取消」<br>3. 进度停止更新 |
| **验证方式** | 状态变为 CANCELLED |

---

## 九、图谱可视化模块

### TC-GRAPH-001：统一图谱页面加载

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-001 |
| **测试场景** | 验证统一图谱页面正常加载 |
| **优先级** | P0 |
| **前置条件** | 项目有已完成的扫描 |
| **测试步骤** | 1. 在项目详情页点击「图谱」→「统一图谱」<br>2. 等待图谱渲染 |
| **预期结果** | 1. 页面标题显示「统一图谱」<br>2. 图谱画布容器可见<br>3. SVG 或 Canvas 图谱渲染成功<br>4. 节点和边可见（有数据时）<br>5. 工具栏可见（缩放、平移、筛选等） |
| **验证方式** | 画布 + 工具栏可见 |

### TC-GRAPH-002：图谱缩放功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-002 |
| **测试场景** | 验证图谱缩放控制 |
| **优先级** | P1 |
| **前置条件** | 图谱已渲染 |
| **测试步骤** | 1. 点击工具栏「放大」按钮（+）<br>2. 点击工具栏「缩小」按钮（-）<br>3. 点击「重置视图」或「适应视图」按钮 |
| **预期结果** | 1. 放大后节点变大，可视区域减少<br>2. 缩小后节点变小，可视区域增大<br>3. 重置后回到初始视图比例 |
| **验证方式** | 图谱变换矩阵变化（视觉检查） |

### TC-GRAPH-003：图谱拖拽平移

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-003 |
| **测试场景** | 验证图谱拖拽平移功能 |
| **优先级** | P1 |
| **前置条件** | 图谱已渲染 |
| **测试步骤** | 1. 在图谱空白处按住鼠标左键<br>2. 拖动鼠标移动<br>3. 松开鼠标 |
| **预期结果** | 1. 图谱跟随鼠标移动<br>2. 松开后停止在新位置 |
| **验证方式** | 视觉检查位置变化 |

### TC-GRAPH-004：节点点击选中

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-004 |
| **测试场景** | 验证点击节点可选中并显示详情 |
| **优先级** | P1 |
| **前置条件** | 图谱中有节点数据 |
| **测试步骤** | 1. 点击任意一个节点<br>2. 观察节点状态和侧边栏 |
| **预期结果** | 1. 被点击节点高亮显示（颜色变化或边框）<br>2. 右侧或底部弹出节点详情面板<br>3. 详情面板显示：节点名称、类型、置信度、证据来源等<br>4. 相关联的边和邻居节点高亮 |
| **验证方式** | 节点高亮 + 详情面板可见 |

### TC-GRAPH-005：节点类型筛选

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-005 |
| **测试场景** | 验证按节点类型筛选显示 |
| **优先级** | P2 |
| **前置条件** | 图谱中有多种类型节点 |
| **测试步骤** | 1. 找到图谱筛选面板或图例<br>2. 取消勾选某一类型（如 Controller）<br>3. 观察图谱变化<br>4. 重新勾选 |
| **预期结果** | 1. 取消勾选后，对应类型节点从图谱中隐藏<br>2. 关联的边也隐藏<br>3. 重新勾选后恢复显示 |
| **验证方式** | 节点数量变化 |

### TC-GRAPH-006：代码图谱页面

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-006 |
| **测试场景** | 验证代码图谱页面 |
| **优先级** | P1 |
| **前置条件** | 项目有代码扫描数据 |
| **测试步骤** | 1. 点击左侧菜单「图谱」→「代码图谱」<br>2. 等待加载 |
| **预期结果** | 1. 页面标题为「代码图谱」<br>2. 显示代码相关节点（Controller、Service、Mapper、表、字段等）<br>3. 节点间有调用关系连线<br>4. 工具栏和筛选功能可用 |
| **验证方式** | 代码图谱渲染成功 |

### TC-GRAPH-007：业务图谱页面

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-007 |
| **测试场景** | 验证业务图谱页面 |
| **优先级** | P1 |
| **前置条件** | 项目有业务图谱数据 |
| **测试步骤** | 1. 点击「图谱」→「业务图谱」<br>2. 等待加载 |
| **预期结果** | 1. 页面标题为「业务图谱」<br>2. 显示业务域、流程、角色、对象等节点<br>3. 节点间有业务关系连线 |
| **验证方式** | 业务图谱渲染成功 |

### TC-GRAPH-008：功能图谱页面

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-008 |
| **测试场景** | 验证功能图谱页面 |
| **优先级** | P2 |
| **前置条件** | 项目有功能扫描数据 |
| **测试步骤** | 1. 点击「图谱」→「功能图谱」<br>2. 等待加载 |
| **预期结果** | 1. 页面标题为「功能图谱」<br>2. 显示页面、按钮、API、权限等节点<br>3. 展示前端功能到后端接口的映射关系 |
| **验证方式** | 功能图谱渲染成功 |

### TC-GRAPH-009：数据血缘图谱

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-009 |
| **测试场景** | 验证数据血缘图谱页面 |
| **优先级** | P2 |
| **前置条件** | 项目有数据库扫描数据 |
| **测试步骤** | 1. 点击「图谱」→「数据血缘」<br>2. 等待加载 |
| **预期结果** | 1. 页面标题为「数据血缘」<br>2. 显示表和字段的血缘关系<br>3. 可追溯字段来源和去向 |
| **验证方式** | 血缘图谱渲染成功 |

### TC-GRAPH-010：运行时图谱

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-010 |
| **测试场景** | 验证运行时图谱页面 |
| **优先级** | P2 |
| **前置条件** | 有运行时数据（可选） |
| **测试步骤** | 1. 点击「图谱」→「运行时图谱」<br>2. 等待加载 |
| **预期结果** | 1. 页面标题为「运行时图谱」<br>2. 显示运行时调用链路数据（如有）<br>3. 空状态有友好提示（如无数据） |
| **验证方式** | 页面正常加载（不报错） |

### TC-GRAPH-011：图谱搜索定位

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-011 |
| **测试场景** | 验证图谱搜索节点功能 |
| **优先级** | P1 |
| **前置条件** | 图谱有数据 |
| **测试步骤** | 1. 找到图谱搜索框<br>2. 输入节点名称关键词<br>3. 选中搜索结果 |
| **预期结果** | 1. 输入时显示匹配的节点列表<br>2. 点击结果后图谱定位到该节点<br>3. 节点居中并高亮 |
| **验证方式** | 节点居中高亮 |

### TC-GRAPH-012：图谱版本差异

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-GRAPH-012 |
| **测试场景** | 验证图谱版本差异对比功能 |
| **优先级** | P2 |
| **前置条件** | 项目有至少两个扫描版本 |
| **测试步骤** | 1. 进入图谱差异页（Graph Diff）<br>2. 选择两个不同的扫描版本<br>3. 点击对比 |
| **预期结果** | 1. 显示新增节点（绿色）、删除节点（红色）、修改节点（黄色）<br>2. 显示统计数字（新增/删除/修改数量）<br>3. 可在图谱上直观看到差异 |
| **验证方式** | 差异统计 + 颜色标识 |

---

## 十、审核管理模块

### TC-REVIEW-001：审核列表页加载

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-REVIEW-001 |
| **测试场景** | 验证审核列表页面 |
| **优先级** | P1 |
| **前置条件** | 进入项目详情 |
| **测试步骤** | 1. 点击左侧菜单「审核」→「审核列表」<br>2. 等待页面加载 |
| **预期结果** | 1. 页面标题「审核列表」<br>2. 有筛选条件（状态、节点类型、置信度范围）<br>3. 待审核项列表展示<br>4. 批量操作按钮（批量通过、批量拒绝） |
| **验证方式** | 列表 + 筛选器可见 |

### TC-REVIEW-002：审核项详情查看

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-REVIEW-002 |
| **测试场景** | 验证点击审核项可查看详情 |
| **优先级** | P1 |
| **前置条件** | 有待审核项 |
| **测试步骤** | 1. 在审核列表点击某一项<br>2. 查看详情面板/弹窗 |
| **预期结果** | 1. 显示节点/关系名称和类型<br>2. 显示置信度分数<br>3. 显示证据来源（文件路径、行号、代码片段等）<br>4. 显示「通过」「拒绝」按钮 |
| **验证方式** | 详情 + 操作按钮可见 |

### TC-REVIEW-003：单条审核通过

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-REVIEW-003 |
| **测试场景** | 验证单条审核通过功能 |
| **优先级** | P1 |
| **前置条件** | 有待审核项 |
| **测试步骤** | 1. 找到一条待审核项<br>2. 点击「通过」或「确认」按钮<br>3. 确认操作（如有弹窗） |
| **预期结果** | 1. 状态变为「已通过」或 CONFIRMED<br>2. 该条目从待审核列表消失（如果筛选的是待审核）<br>3. 显示成功提示 |
| **验证方式** | 状态变更 + 成功提示 |

### TC-REVIEW-004：单条审核拒绝

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-REVIEW-004 |
| **测试场景** | 验证单条审核拒绝功能 |
| **优先级** | P2 |
| **前置条件** | 有待审核项 |
| **测试步骤** | 1. 找到一条待审核项<br>2. 点击「拒绝」按钮<br>3. 填写拒绝原因（如有）<br>4. 确认 |
| **预期结果** | 1. 状态变为「已拒绝」或 REJECTED<br>2. 显示成功提示 |
| **验证方式** | 状态变更为 REJECTED |

### TC-REVIEW-005：批量审核

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-REVIEW-005 |
| **测试场景** | 验证批量审核功能 |
| **优先级** | P2 |
| **前置条件** | 有待审核项（至少 2 条） |
| **测试步骤** | 1. 勾选多条待审核项（复选框）<br>2. 点击「批量通过」按钮<br>3. 确认操作 |
| **预期结果** | 1. 所有勾选项状态变为已通过<br>2. 显示批量操作成功提示（成功 N 条）<br>3. 勾选的项从列表消失 |
| **验证方式** | 多条数据状态同时变更 |

### TC-REVIEW-006：审核历史记录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-REVIEW-006 |
| **测试场景** | 验证审核历史记录页面 |
| **优先级** | P2 |
| **前置条件** | 有已审核的记录 |
| **测试步骤** | 1. 点击「审核」→「审核历史」<br>2. 查看列表 |
| **预期结果** | 1. 显示已审核的记录列表<br>2. 包含审核人、审核时间、审核结果（通过/拒绝）<br>3. 可按时间、审核人筛选<br>4. 可查看审核详情 |
| **验证方式** | 历史记录列表可见 |

---

## 十一、事实与证据模块

### TC-FACT-001：事实列表页

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-FACT-001 |
| **测试场景** | 验证事实列表页面 |
| **优先级** | P1 |
| **前置条件** | 项目有事实数据 |
| **测试步骤** | 1. 点击左侧菜单「事实」<br>2. 等待页面加载 |
| **预期结果** | 1. 页面标题「事实列表」<br>2. 有搜索/筛选功能<br>3. 事实列表展示（陈述式事实）<br>4. 每条事实有置信度和来源 |
| **验证方式** | 列表 + 筛选器可见 |

### TC-FACT-002：证据搜索功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-FACT-002 |
| **测试场景** | 验证证据搜索功能 |
| **优先级** | P1 |
| **前置条件** | 有图谱数据 |
| **测试步骤** | 1. 点击「证据」或进入证据搜索页<br>2. 在搜索框输入关键词（如「用户」「登录」）<br>3. 点击搜索 |
| **预期结果** | 1. 返回相关的证据片段列表<br>2. 每条证据显示来源文档/文件、位置、相关节点<br>3. 关键词高亮显示<br>4. 可点击查看完整证据 |
| **验证方式** | 搜索结果非空（有匹配数据时） |

### TC-FACT-003：证据详情查看

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-FACT-003 |
| **测试场景** | 验证证据详情展示 |
| **优先级** | P2 |
| **前置条件** | 有证据数据 |
| **测试步骤** | 1. 在证据列表点击某条证据<br>2. 查看详情 |
| **预期结果** | 1. 显示完整证据内容（代码片段/文档片段）<br>2. 显示来源信息（文件、行号）<br>3. 显示关联的图谱节点<br>4. 支持跳转到对应图谱节点 |
| **验证方式** | 详情内容完整 |

---

## 十二、测试用例管理模块

### TC-TEST-001：测试用例列表页

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-TEST-001 |
| **测试场景** | 验证测试用例列表页面 |
| **优先级** | P1 |
| **前置条件** | 项目有测试用例数据 |
| **测试步骤** | 1. 点击左侧菜单「测试」→「测试用例」<br>2. 等待页面加载 |
| **预期结果** | 1. 页面标题「测试用例」<br>2. 有「新建用例」按钮<br>3. 用例列表展示（名称、类型、状态、关联功能）<br>4. 有筛选和搜索功能 |
| **验证方式** | 列表 + 新建按钮可见 |

### TC-TEST-002：测试用例编辑器

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-TEST-002 |
| **测试场景** | 验证测试用例编辑器页面 |
| **优先级** | P2 |
| **前置条件** | 进入用例列表页 |
| **测试步骤** | 1. 点击「新建用例」按钮<br>2. 进入编辑器页面 |
| **预期结果** | 1. 显示用例基本信息表单（名称、描述、类型）<br>2. 显示步骤编辑区域<br>3. 显示断言配置区域<br>4. 有保存和取消按钮 |
| **验证方式** | 编辑器表单完整 |

### TC-TEST-003：测试运行列表

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-TEST-003 |
| **测试场景** | 验证测试运行列表页面 |
| **优先级** | P2 |
| **前置条件** | 有测试运行记录 |
| **测试步骤** | 1. 点击「测试」→「测试运行」<br>2. 查看列表 |
| **预期结果** | 1. 显示历史测试运行记录<br>2. 每条记录显示：运行编号、触发时间、状态、通过/失败数<br>3. 可点击查看运行详情 |
| **验证方式** | 运行列表可见 |

### TC-TEST-004：测试运行详情

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-TEST-004 |
| **测试场景** | 验证测试运行详情页面 |
| **优先级** | P2 |
| **前置条件** | 有测试运行记录 |
| **测试步骤** | 1. 点击某条测试运行记录<br>2. 查看详情 |
| **预期结果** | 1. 显示运行概览（总数、通过、失败、耗时）<br>2. 显示各用例执行结果列表<br>3. 失败用例可展开查看错误详情<br>4. 有重新运行按钮 |
| **验证方式** | 详情页各区块可见 |

---

## 十三、QA 问答模块

### TC-QA-001：QA 问答页面加载

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-QA-001 |
| **测试场景** | 验证图谱问答页面正常加载 |
| **优先级** | P0 |
| **前置条件** | 项目有图谱数据 |
| **测试步骤** | 1. 点击左侧菜单「QA 问答」或「图谱问答」<br>2. 等待页面加载 |
| **预期结果** | 1. 页面标题「图谱问答」或「QA」<br>2. 左侧/顶部有问题输入框<br>3. 有对话历史区域（左侧或右侧）<br>4. 输入框有发送按钮 |
| **验证方式** | 输入框 + 对话区域可见 |

### TC-QA-002：发送问题获取回答

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-QA-002 |
| **测试场景** | 验证发送问题后能收到回答 |
| **优先级** | P0 |
| **前置条件** | LLM 服务可用，有图谱数据 |
| **测试步骤** | 1. 在输入框输入问题：「这个系统有哪些主要功能？」<br>2. 点击发送按钮或按回车<br>3. 等待回答生成 |
| **预期结果** | 1. 用户消息出现在对话区域<br>2. 显示「正在思考...」或加载动画<br>3. AI 回答在几秒内返回<br>4. 回答包含相关内容（非空）<br>5. 回答附带有证据来源或引用节点 |
| **验证方式** | AI 回复非空 + 有证据引用 |

### TC-QA-003：流式回答展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-QA-003 |
| **测试场景** | 验证流式输出效果（SSE） |
| **优先级** | P1 |
| **前置条件** | QA 支持流式输出 |
| **测试步骤** | 1. 发送一个稍复杂的问题<br>2. 观察回答生成过程 |
| **预期结果** | 1. 回答逐字/逐段显示（流式输出）<br>2. 光标或加载指示器在生成时可见<br>3. 生成完成后停止，显示完整答案 |
| **验证方式** | 视觉检查流式输出效果 |

### TC-QA-004：回答反馈功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-QA-004 |
| **测试场景** | 验证回答的点赞/点踩反馈 |
| **优先级** | P2 |
| **前置条件** | 有一条 AI 回答 |
| **测试步骤** | 1. 找到一条 AI 回答<br>2. 点击「👍」有用按钮<br>3. 点击「👎」没用按钮 |
| **预期结果** | 1. 点赞后按钮状态变化（高亮）<br>2. 点踩后按钮状态变化<br>3. 显示「感谢反馈」提示（如有） |
| **验证方式** | 按钮状态变化 |

### TC-QA-005：对话历史保存

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-QA-005 |
| **测试场景** | 验证对话历史记录保存 |
| **优先级** | P2 |
| **前置条件** | 已进行多轮对话 |
| **测试步骤** | 1. 发送多个问题形成对话历史<br>2. 刷新页面<br>3. 检查对话历史是否保留 |
| **预期结果** | 1. 刷新页面后对话历史仍然存在<br>2. 可以点击历史会话切换<br>3. 可以新建对话 |
| **验证方式** | 刷新后历史保留 |

### TC-QA-006：相关节点展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-QA-006 |
| **测试场景** | 验证回答关联的图谱节点展示 |
| **优先级** | P1 |
| **前置条件** | 有一条带引用的回答 |
| **测试步骤** | 1. 发送一个能引用到图谱节点的问题<br>2. 在回答中找到引用的节点链接<br>3. 点击节点链接 |
| **预期结果** | 1. 回答中有「相关节点」或「证据来源」区块<br>2. 列出引用的节点名称和类型<br>3. 点击节点可跳转到图谱并定位到该节点 |
| **验证方式** | 引用节点可见 + 跳转正确 |

---

## 十四、Agent Hub 模块

### TC-AGENT-001：Agent Hub 页面加载

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AGENT-001 |
| **测试场景** | 验证 Agent Hub 页面正常加载 |
| **优先级** | P1 |
| **前置条件** | 已登录 |
| **测试步骤** | 1. 点击左侧菜单「Agent Hub」<br>2. 等待页面加载 |
| **预期结果** | 1. 页面标题「Agent Hub」<br>2. 显示可用的 Agent 列表（卡片或列表形式）<br>3. 每个 Agent 有名称、描述、图标<br>4. 有「运行」或「使用」按钮 |
| **验证方式** | Agent 列表可见 |

### TC-AGENT-002：Agent 列表展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AGENT-002 |
| **测试场景** | 验证 Agent 卡片信息完整性 |
| **优先级** | P2 |
| **前置条件** | 在 Agent Hub 页面 |
| **测试步骤** | 1. 浏览 Agent 列表<br>2. 检查每个卡片内容 |
| **预期结果** | 1. Agent 名称清晰可见<br>2. 有简短功能描述<br>3. 有分类标签（代码分析、测试生成、文档理解等）<br>4. 有使用次数或评分（如有） |
| **验证方式** | 卡片信息完整 |

### TC-AGENT-003：Agent 运行功能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AGENT-003 |
| **测试场景** | 验证点击运行 Agent 功能 |
| **优先级** | P2 |
| **前置条件** | Agent 服务可用 |
| **测试步骤** | 1. 选择一个 Agent（如代码审查 Agent）<br>2. 点击「运行」按钮<br>3. 填写必要参数<br>4. 确认运行 |
| **预期结果** | 1. 跳转到 Agent 运行页面或对话框<br>2. 显示运行进度<br>3. 运行完成后显示结果 |
| **验证方式** | 运行流程完整 |

### TC-AGENT-004：Agent 历史记录

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-AGENT-004 |
| **测试场景** | 验证 Agent 运行历史记录 |
| **优先级** | P2 |
| **前置条件** | 有 Agent 运行记录 |
| **测试步骤** | 1. 点击「Agent 历史」或查看运行记录<br>2. 浏览历史列表 |
| **预期结果** | 1. 显示历史运行记录列表<br>2. 每条记录有：Agent 名称、触发时间、状态、耗时<br>3. 可点击查看运行详情和结果 |
| **验证方式** | 历史列表可见 |

---

## 十五、迁移风险模块

### TC-RISK-001：迁移风险列表页

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-RISK-001 |
| **测试场景** | 验证迁移风险列表页面 |
| **优先级** | P1 |
| **前置条件** | 项目有迁移风险数据 |
| **测试步骤** | 1. 点击左侧菜单「迁移风险」<br>2. 等待页面加载 |
| **预期结果** | 1. 页面标题「迁移风险」<br>2. 风险统计概览（高/中/低风险数量）<br>3. 风险列表展示<br>4. 可按风险等级筛选 |
| **验证方式** | 列表 + 统计卡片可见 |

### TC-RISK-002：风险详情查看

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-RISK-002 |
| **测试场景** | 验证风险详情页面 |
| **优先级** | P2 |
| **前置条件** | 有风险项 |
| **测试步骤** | 1. 点击某条风险项<br>2. 查看详情 |
| **预期结果** | 1. 显示风险类型和等级<br>2. 显示受影响的节点/模块<br>3. 显示风险描述和建议<br>4. 有「标记已处理」按钮 |
| **验证方式** | 详情内容完整 |

### TC-RISK-003：风险等级筛选

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-RISK-003 |
| **测试场景** | 验证按风险等级筛选 |
| **优先级** | P2 |
| **前置条件** | 有不同等级的风险 |
| **测试步骤** | 1. 点击筛选器选择「高风险」<br>2. 观察列表变化<br>3. 再选择「全部」 |
| **预期结果** | 1. 筛选高风险后列表只显示高风险项<br>2. 恢复全部后显示所有风险 |
| **验证方式** | 列表数据与筛选条件一致 |

---

## 十六、系统管理模块

### TC-SYS-001：用户管理列表

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-001 |
| **测试场景** | 验证用户管理列表页面 |
| **优先级** | P1 |
| **前置条件** | 已用管理员账号登录 |
| **测试步骤** | 1. 点击「系统管理」→「用户管理」<br>2. 等待页面加载 |
| **预期结果** | 1. 页面标题「用户管理」<br>2. 有「添加用户」按钮<br>3. 用户列表显示：用户名、昵称、角色、状态、创建时间<br>4. 操作列有编辑、禁用/启用、删除 |
| **验证方式** | 列表 + 添加按钮可见 |

### TC-SYS-002：添加新用户

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-002 |
| **测试场景** | 验证添加新用户功能 |
| **优先级** | P1 |
| **前置条件** | 在用户管理页（管理员权限） |
| **测试步骤** | 1. 点击「添加用户」<br>2. 填写用户名：testuser01<br>3. 填写昵称：测试用户01<br>4. 填写邮箱：test01@example.com<br>5. 设置密码：Test@1234<br>6. 选择角色：普通用户<br>7. 点击「确认」 |
| **预期结果** | 1. 成功提示消息<br>2. 用户列表新增一条记录<br>3. 新用户状态为「正常」 |
| **验证方式** | 列表新增用户 |

### TC-SYS-003：用户禁用/启用

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-003 |
| **测试场景** | 验证用户禁用和启用功能 |
| **优先级** | P2 |
| **前置条件** | 有可操作的测试用户 |
| **测试步骤** | 1. 找到测试用户，点击「禁用」<br>2. 确认禁用<br>3. 观察状态变化<br>4. 再点击「启用」 |
| **预期结果** | 1. 禁用后状态变为「已禁用」<br>2. 启用后状态恢复「正常」<br>3. 禁用的用户无法登录 |
| **验证方式** | 状态标签切换 |

### TC-SYS-004：字典管理

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-004 |
| **测试场景** | 验证字典管理页面 |
| **优先级** | P2 |
| **前置条件** | 管理员登录 |
| **测试步骤** | 1. 点击「系统管理」→「字典管理」<br>2. 查看字典列表 |
| **预期结果** | 1. 显示字典分类列表<br>2. 点击某字典可查看字典项<br>3. 可新增、编辑、删除字典项 |
| **验证方式** | 字典列表 + 详情可见 |

### TC-SYS-005：系统设置页面

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-005 |
| **测试场景** | 验证系统设置页面 |
| **优先级** | P2 |
| **前置条件** | 管理员登录 |
| **测试步骤** | 1. 点击「系统管理」→「系统设置」<br>2. 浏览设置项 |
| **预期结果** | 1. 显示系统配置项（扫描配置、LLM 配置、存储配置等）<br>2. 可修改配置并保存<br>3. 保存后有成功提示 |
| **验证方式** | 设置项可编辑 + 保存成功 |

### TC-SYS-006：LLM Provider 配置

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-006 |
| **测试场景** | 验证 LLM 模型提供商配置 |
| **优先级** | P1 |
| **前置条件** | 管理员登录 |
| **测试步骤** | 1. 点击「系统管理」→「LLM 配置」或「模型提供商」<br>2. 查看 Provider 列表 |
| **预期结果** | 1. 显示已配置的 LLM Provider 列表<br>2. 有「添加 Provider」按钮<br>3. 每个 Provider 显示：名称、模型类型、状态<br>4. 可测试连接 |
| **验证方式** | Provider 列表可见 |

### TC-SYS-007：Prompt 模板管理

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-007 |
| **测试场景** | 验证 Prompt 模板管理页面 |
| **优先级** | P2 |
| **前置条件** | 管理员登录 |
| **测试步骤** | 1. 点击「系统管理」→「Prompt 模板」<br>2. 查看模板列表 |
| **预期结果** | 1. 显示系统内置和自定义的 Prompt 模板<br>2. 可按用途筛选（QA、代码分析、测试生成等）<br>3. 可编辑模板内容<br>4. 有版本历史（如有） |
| **验证方式** | 模板列表 + 编辑器可见 |

### TC-SYS-008：插件管理

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-008 |
| **测试场景** | 验证插件管理页面 |
| **优先级** | P2 |
| **前置条件** | 管理员登录 |
| **测试步骤** | 1. 点击「系统管理」→「插件管理」<br>2. 查看插件列表 |
| **预期结果** | 1. 显示已安装的插件列表<br>2. 每个插件显示：名称、版本、状态、描述<br>3. 可启用/禁用插件<br>4. 有「安装插件」或「上传插件」按钮 |
| **验证方式** | 插件列表可见 |

### TC-SYS-009：审计日志列表

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-009 |
| **测试场景** | 验证审计日志列表页面 |
| **优先级** | P2 |
| **前置条件** | 管理员登录 |
| **测试步骤** | 1. 点击「系统管理」→「审计日志」<br>2. 查看日志列表 |
| **预期结果** | 1. 显示操作日志列表<br>2. 每条记录有：操作人、操作类型、操作时间、IP 地址、结果<br>3. 可按时间范围、操作人、操作类型筛选<br>4. 可查看日志详情 |
| **验证方式** | 日志列表 + 筛选器可见 |

### TC-SYS-010：审计日志详情

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-SYS-010 |
| **测试场景** | 验证审计日志详情查看 |
| **优先级** | P3 |
| **前置条件** | 在审计日志列表页 |
| **测试步骤** | 1. 点击某条日志记录的「详情」<br>2. 查看详情弹窗/页面 |
| **预期结果** | 1. 显示完整的操作信息<br>2. 显示请求参数（脱敏后）<br>3. 显示响应结果摘要<br>4. 显示客户端信息（User-Agent、IP 等） |
| **验证方式** | 详情内容完整 |

---

## 十七、异常与边界测试

### TC-EDGE-001：404 页面

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-EDGE-001 |
| **测试场景** | 验证访问不存在的路由显示 404 页面 |
| **优先级** | P2 |
| **前置条件** | 已登录 |
| **测试步骤** | 1. 在地址栏输入一个不存在的路径，如 /abc123xyz<br>2. 回车访问 |
| **预期结果** | 1. 显示 404 页面<br>2. 有「返回首页」或「返回上一页」按钮<br>3. 页面样式统一，不报错 |
| **验证方式** | 404 页面可见 + 返回按钮可用 |

### TC-EDGE-002：403 权限不足页面

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-EDGE-002 |
| **测试场景** | 验证无权限访问时显示 403 页面 |
| **优先级** | P2 |
| **前置条件** | 有普通用户账号（非管理员） |
| **测试步骤** | 1. 使用普通用户登录<br>2. 直接访问 /system/users（系统管理用户页） |
| **预期结果** | 1. 跳转到 403 页面<br>2. 显示「无权限访问」或类似提示<br>3. 有「返回首页」按钮 |
| **验证方式** | 403 页面可见 |

### TC-EDGE-003：空数据状态展示

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-EDGE-003 |
| **测试场景** | 验证各列表页无数据时空状态展示 |
| **优先级** | P2 |
| **前置条件** | 新建一个空项目 |
| **测试步骤** | 1. 进入空项目<br>2. 依次访问：代码仓库、数据库、文档、扫描版本、图谱各页面、事实、测试用例 |
| **预期结果** | 1. 每个页面在无数据时显示友好的空状态提示<br>2. 有空状态图标和说明文字<br>3. 有引导操作按钮（如「去添加」「去创建」）<br>4. 页面布局正常，不崩溃 |
| **验证方式** | 各页面空状态友好展示 |

### TC-EDGE-004：网络异常处理

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-EDGE-004 |
| **测试场景** | 验证后端服务不可用时的错误处理 |
| **优先级** | P2 |
| **前置条件** | 已登录 |
| **测试步骤** | 1. 停止后端服务（或断开网络）<br>2. 在前端点击任意需要请求后端的操作<br>3. 观察页面表现 |
| **预期结果** | 1. 显示错误提示消息（红色，如「网络错误，请稍后重试」）<br>2. 页面不崩溃白屏<br>3. 有重试机制或按钮（如有） |
| **验证方式** | 错误提示友好 + 页面可用 |

### TC-EDGE-005：表单输入边界值

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-EDGE-005 |
| **测试场景** | 验证表单输入的边界值处理 |
| **优先级** | P3 |
| **前置条件** | 打开任意表单（如新建项目） |
| **测试步骤** | 1. 输入超长文本（超过最大长度限制）<br>2. 输入特殊字符（SQL 注入、XSS 脚本）<br>3. 输入纯空格<br>4. 提交表单 |
| **预期结果** | 1. 超长文本有长度限制或截断提示<br>2. 特殊字符被正确转义，不引起 XSS<br>3. 纯空格触发必填校验（trim 后为空）<br>4. 表单不提交成功（不符合要求时） |
| **验证方式** | 各类边界输入正确处理 |

### TC-EDGE-006：分页边界测试

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-EDGE-006 |
| **测试场景** | 验证列表分页的边界情况 |
| **优先级** | P2 |
| **前置条件** | 数据量超过一页 |
| **测试步骤** | 1. 进入有分页的列表页<br>2. 点击「下一页」直到最后一页<br>3. 点击「上一页」回到第一页<br>4. 跳转到第 0 页或超最大页（如存在输入框） |
| **预期结果** | 1. 第一页时「上一页」和「首页」按钮禁用<br>2. 最后一页时「下一页」和「末页」按钮禁用<br>3. 页码超出范围时自动调整到有效范围<br>4. 分页器显示正确的总页数和总数 |
| **验证方式** | 分页边界按钮状态正确 |

---

## 十八、性能与兼容性测试

### TC-PERF-001：首屏加载时间

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PERF-001 |
| **测试场景** | 验证应用首屏加载性能 |
| **优先级** | P2 |
| **前置条件** | 清空浏览器缓存 |
| **测试步骤** | 1. 打开浏览器开发者工具（Network 面板）<br>2. 访问应用首页<br>3. 记录加载时间 |
| **预期结果** | 1. 首屏有内容（FCP）在 2 秒内<br>2. 主要资源加载完成在 3 秒内<br>3. 无阻塞性的长任务（>50ms）过多 |
| **验证方式** | Performance API 或 Network 面板 |

### TC-PERF-002：大图渲染性能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PERF-002 |
| **测试场景** | 验证大规模图谱（1000+ 节点）的渲染性能 |
| **优先级** | P3 |
| **前置条件** | 有大数据量图谱 |
| **测试步骤** | 1. 进入图谱页面<br>2. 观察渲染耗时<br>3. 尝试缩放、拖拽操作 |
| **预期结果** | 1. 图谱渲染完成时间可接受（< 5 秒）<br>2. 缩放和拖拽操作流畅（无明显卡顿）<br>3. 浏览器内存不持续暴涨（无内存泄漏） |
| **验证方式** | 视觉流畅度 + 内存检查 |

### TC-PERF-003：长列表滚动性能

| 项目 | 内容 |
|------|------|
| **用例编号** | TC-PERF-003 |
| **测试场景** | 验证长列表滚动流畅度 |
| **优先级** | P3 |
| **前置条件** | 有超过 100 条数据的列表 |
| **测试步骤** | 1. 进入长列表页面<br>2. 快速上下滚动<br>3. 观察滚动流畅度 |
| **预期结果** | 1. 滚动流畅，无明显掉帧<br>2. 如有虚拟滚动，滚动条行为正常<br>3. 不触发浏览器崩溃或无响应 |
| **验证方式** | 视觉流畅度检查 |

---

## 附录 A：测试用例执行记录模板

| 用例编号 | 模块 | 测试场景 | 优先级 | 执行人 | 执行日期 | 结果（通过/失败/阻塞） | 备注 |
|----------|------|----------|--------|--------|----------|----------------------|------|
| TC-SMOKE-001 | 冒烟 | 应用启动与首页加载 | P0 | | | | |
| TC-SMOKE-002 | 冒烟 | 登录后进入主应用 | P0 | | | | |
| ... | ... | ... | ... | ... | ... | ... | ... |

## 附录 B：缺陷报告模板

- **缺陷标题**：简洁描述问题
- **所属模块**：如登录、图谱、QA 等
- **用例编号**：关联的测试用例
- **严重级别**：P0 / P1 / P2 / P3
- **前置条件**：复现前的环境和数据
- **复现步骤**：1、2、3... 详细步骤
- **预期结果**：应该发生什么
- **实际结果**：实际发生了什么
- **截图/录屏**：附件
- **环境信息**：浏览器版本、系统版本
- **备注**：其他补充信息

---

> 文档版本：v1.0
> 编写日期：2026-07-12
> 适用版本：LegacyGraph v1.x
