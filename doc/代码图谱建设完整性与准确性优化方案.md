# 代码图谱建设完整性与准确性优化方案

> 2026-07-08 更新：基于保证金项目 #14（优化前）→ #15（优化后）两次扫描实测对比，量化本轮 15 项代码改动的实际效果，并识别剩余优化空间。

## 0. 本轮优化实测效果（保证金项目 #14 → #15）

### 0.1 扫描总览

| 指标 | #14 (07-08 10:42) | #15 (07-08 15:11) | 变化 |
|------|-------------------|-------------------|------|
| 总耗时 | 771s | 774s | +3s（持平） |
| 节点数 | 2,293 | 2,328 | **+35 (+1.5%)** |
| 边数 (Neo4j) | 3,935 | ~4,100 | **+165 (+4.2%)** |
| 事实数 | 755 | 842 | +87 |

### 0.2 步骤耗时对比

| 步骤 | #14 | #15 | 变化 | 说明 |
|------|-----|-----|------|------|
| ADAPTER_SCAN | 219.9s | 222.1s | +2.2s | ✅ 稳定 |
| AI_DOC_EXTRACT | 68.1s | 70.9s | +2.8s | ✅ 稳定，A3 分段抽取未触发（无大文档） |
| AI_CODE_EXTRACT | 92.4s | 107.2s | +14.8s | 事实 79→83，略增 |
| AI_FEATURE_CODE_MAPPING | 205.7s | 209.5s | +3.8s | 映射 1871→1946 (+75) |
| AI_FEATURE_MAPPING | 109.7s | 163.9s | +54.2s | 🔥 映射 22→48，**B1 向量语义生效** |
| AI_GAP_FINDING | 51.1s | 42.8s | -8.3s | 缺口 883→813 |
| MEMBER_CALL_RESOLVE | 5.3s | 5.9s | +0.6s | ⚠️ 仍 resolved 0 edges |

### 0.3 边类型变化

| 边类型 | #14 | #15 | 变化 | 评估 |
|--------|-----|-----|------|------|
| IMPLEMENTED_BY | 1,916 | 1,999 | **+83** | 🔥 Feature→API 映射增强 |
| CONTAINS | 1,461 | 1,474 | +13 | 微增 |
| HANDLED_BY | 198 | 202 | +4 | API→Method |
| **EXPOSED_BY** | **6** | **44** | **+38 (+633%)** | 🔥🔥 B1 向量映射生效，Feature→Page 大幅恢复 |
| MAPS_TO | 21 | 21 | 0 | 稳定 |
| READS/WRITES/EXECUTES/JOINS | 328 | 328 | 0 | 稳定 |
| **CALLS** | **0** | **0** | 0 | ⚠️ 仍未恢复，见 §0.6 |
| CALLS_EXTERNAL | 1 | 1 | 0 | 稳定 |

### 0.4 节点类型变化

| 节点类型 | #14 | #15 | 变化 |
|----------|-----|-----|------|
| Feature | 569 | 573 | +4 |
| ApiEndpoint | 193 | 197 | +4 |
| Method | 1,023 | 1,038 | +15 |
| Service | 84 | 83 | -1 |
| 其他 | 424 | 437 | +13 |

### 0.5 业务层连通性

| 节点类型 | 数量 | 边数 | 孤立率 | 状态 |
|----------|------|------|--------|------|
| BusinessProcess | 49 | 105 | 0% | ✅ |
| BusinessObject | 33 | 58 | 0% | ✅ |
| BusinessDomain | 37 | 0 | **100%** | ⚠️ `mapBusinessDomainsToCode` 未部署 |
| BusinessRule | 29 | 0 | **100%** | ⚠️ 缺连接路径 |
| Role | 15 | 0 | **100%** | ⚠️ 缺连接路径 |

### 0.6 CALLS 边恢复方案

**已实施**：扩 `JavaMemberCallResolver` 为双路径模式：

| 路径 | 适用场景 | 逻辑 |
|------|---------|------|
| 正向（原有） | targetClass 已由 AST 解析（注解注入） | god-node 精确匹配 |
| **反向（新增）** | targetClass 为 null（XML 配置注入） | calledMethod 名全局搜索 → god-node(恰好1) → 同包消歧 |

反向匹配置信度 0.85 + PENDING_CONFIRM（略低于正向的 1.0 + CONFIRMED，确保人工可审核）。

**预期效果**：480 条 SERVICE_CALL 中，calledMethod 在全局 Method 索引中唯一命中的调用将被解析为 CALLS 边。对于保证金项目（1,038 个 Method 节点），同名方法占比取决于代码风格——getById/findById 等通用名可能歧义跳过，insert*/update*/delete* 等业务方法名大概率唯一命中。

### 0.7 已生效的优化

| 优化项 | 文件 | 效果 |
|--------|------|------|
| B1 向量语义映射 | `BusinessGraphBuilder.java` | EXPOSED_BY 6→44 (+633%)，IMPLEMENTED_BY +83 |
| 表名大小写归一化 | `SqlTableExtractor.java` + `GraphBuilder.java` | 待项目有 SQL 大小写混合场景时验证 |
| 线程池虚拟线程化 | `AiScanStepSupport.java` | 8 个固定 OS 线程→虚拟线程按需创建 |
| codeExtractExecutor 独立 | `AiScanStepSupport.java` + `CodeExtractStep.java` | doc/code 各独立 4 路 |
| edgeType 属性修复 | `Neo4jWriteRepository.java` | 新边不再 null，存量已回填 |
| A3 大文档分段抽取 | `DocExtractStep.java` | 代码就绪，保证金文档均 ≤8000 字符未触发 |
| 业务域映射 | `BusinessGraphBuilder.java` | 代码就绪，**本次扫描未部署** |

### 0.8 未生效的优化（待部署重扫）

| 优化项 | 原因 |
|--------|------|
| CALLS 边恢复 | `ServiceCallExtractor` 修复不足以覆盖 XML 配置注入项目，需 P1 证据驱动方案 |
| 业务域→代码映射 | `mapBusinessDomainsToCode` 代码就绪但本次扫描未重新编译部署 |
| A3 文档分段 | 保证金文档均小于 8000 字符，未触发分段逻辑 |

---

## 1. 当前状态判断

### 1.1 已具备的基础

1. `JavaMemberCallResolver` 已在 `ADAPTER_SCAN` 后运行二次解析，模式接近 Graphify 的语言 resolver：先全量建索引，再用 god-node guard 只补确定性 `CALLS` 边。
2. `GraphBuilder.buildServiceCallGraph` 已改成目标 find-only，避免因简单名/FQN 不一致创建重复节点。
3. `ServiceCallExtractor` 已能收集字段注入、构造器注入、Lombok `final` 字段、方法参数和本地变量类型，并能生成部分被调方法签名。
4. MyBatis XML 链路已经有 `Mapper -EXECUTES-> SqlStatement -READS/WRITES-> Table`，`buildMapperSqlGraph` 会优先解析展开 include 后的 SQL。
5. 项目已有 Graphify 外部分析入口、Graphify canonical mapper、质量报告、Gap 扫描、工具运行新鲜度检查等基础设施。
6. **本轮新增**：B1 向量语义映射（max(tokenScore, cosineSimilarity)）已生效，EXPOSED_BY +633%。
7. **本轮新增**：表名大小写归一化（JSqlParser + 正则双路径 lowerCase）。
8. **本轮新增**：线程池虚拟线程化（`boundedVirtualExecutor`）。
9. **本轮新增**：大文档分段抽取（并行 LLM + 合并去重）。

### 1.2 仍然影响完整性/准确性的核心缺口

| 缺口 | 现状 | 本轮进展 | 风险 |
|------|------|---------|------|
| CALLS 边对 XML 配置项目无效 | `ServiceCallExtractor` 依赖注解识别注入，XML `<bean>` 项目全部漏 | 🔴 已定位根因，修复方案见 §1.2.1 | 传统 Spring 项目调用链断裂 |
| 接收者类型没有作为一等证据保存 | `CallRelationDto` 只保存 caller/targetClass/method/signature | 🟡 未做 | 无法区分"未解析""歧义跳过" |
| FQN 消歧只靠简单名唯一 | `simpleNameToClassNodes` 多候选即跳过 | 🟡 未做 | 同名类多的老系统大量漏边 |
| 方法签名归一规则分散 | `JavaStructureExtractor`、`ServiceCallExtractor` 重复归一逻辑 | 🟡 未做 | method nodeKey 漂移 |
| BusinessDomain/Rule/Role 孤立 | 100% 孤立率 | 🟢 `mapBusinessDomainsToCode` 已写，待部署 | 业务层与技术层割裂 |
| 缺少端到端图谱质量门禁 | 质量报告看总节点/总边 | 🟡 未做 | 无法回答"Controller→Table 覆盖多少" |

#### 1.2.1 CALLS 边恢复方案（替代 ServiceCallExtractor 修补）

当前 `ServiceCallExtractor` 的 AST 级别注入推断对注解项目有效，但对 XML 配置项目（如保证金）完全失效。正确路径不是继续修补 AST 推断，而是：

1. **扩 `JavaMemberCallResolver`**：在全局索引阶段，对每个 `SERVICE_CALL` fact（无论 targetClass 是否为 null），利用事实中的 `callerClass` + `calledMethod` + 全局类/方法索引做反向匹配：
   - 若 `calledMethod` 在全局惟一定义→直接解析
   - 若多个同名方法→利用调用方 FQN 的 import/同包做消歧
2. **降低对文件级注入推断的依赖**：`SERVICE_CALL` fact 只需记录"谁调了哪个方法"，不要求 targetClass 已解析；全局解析阶段负责消歧。

## 2. 对标开源项目后的可借鉴点

（同原文档 §2，保持不变）

## 3. 优化目标

### 3.1 本轮已验证达标

| 目标 | 优化前 | 优化后 | 状态 |
|------|--------|--------|------|
| Feature→API 映射 | 1,916 | 1,999 | ✅ B1 向量映射 |
| Feature→Page 暴露 | 6 | 44 | ✅ +633% |
| 扫描耗时 | 771s | 774s | ✅ 持平 |
| 线程资源 | 8 OS 线程 | 虚拟线程按需 | ✅ |
| Doc/Code 线程池独立 | 共用 1 池 | 各独立 4 路 | ✅ |

### 3.2 仍待达成

| 目标 | 当前值 | 目标值 |
|------|--------|--------|
| CALLS 边（Service→Mapper） | 0 | ≥14 |
| BusinessDomain 连通率 | 0% | ≥50% |
| BusinessRule 连通率 | 0% | ≥30% |
| 完整链路 Controller→Table | 0 | ≥5 |

## 4. 分阶段实施方案

### P0：补质量度量 + 修 CALLS 边（当前阶段）

**P0.1** 扩 `JavaMemberCallResolver` 为反向匹配模式：
- 对每条 `SERVICE_CALL` fact，不依赖 `targetClass` 是否已解析
- 在全局 Method 索引中按 `calledMethod` 名称查找
- 唯一命中→建边；多候选→利用同包/import 消歧

**P0.2** 部署 `mapBusinessDomainsToCode`，验证 BusinessDomain 孤立率下降。

**P0.3** 新增 `GraphCompletenessAuditService`（见原方案 P0.1）。

### P1-P6

（保持原方案，本轮未触及）

## 5. 建议排期

| 阶段 | 状态 |
|------|------|
| 本轮 15 项优化 | ✅ 已完成 |
| P0 CALLS 反向匹配 | 🟢 **已实现** — `JavaMemberCallResolver.resolveByMethodName` 双路径 |
| P0 mapBusinessDomainsToCode 部署 | 🔴 待部署重扫 |
| P0.3 质量门禁 | ⚫ 待排期 |
| P1 证据驱动 resolver | ⚫ 待排期 |
| P2-P6 | ⚫ 待排期 |

## 6. 立即可执行的任务清单

1. ✅ ~~扩 `ServiceCallExtractor.CallRelation`~~ → 已做（methodVarToType），但对 XML 配置项目无效
2. ✅ **扩 `JavaMemberCallResolver` 为反向匹配**：`resolveByMethodName` — calledMethod 名全局搜索 + god-node + 同包消歧
3. ✅ **跨语言 Feature 去重**：`mergeCrossLanguageFeatures` — DOC_AI↔FRONTEND_AST 向量语义合并，>0.75 迁移边+删冗余节点
4. 🔴 **部署重扫**：`mapBusinessDomainsToCode` + 表名归一化 + A3 分段 + 虚拟线程 + B1 向量 + CALLS 反向匹配 + Feature 去重
4. ⚫ 新建 `GraphCompletenessAuditService`
5. ⚫ 方法签名统一工具类
6. ⚫ 黄金 fixture 项目

## 7. 推荐验收指标

| 指标 | #15 当前 | 目标 |
|------|---------|------|
| CALLS 边 (Service→Mapper) | 0 | ≥14 |
| EXPOSED_BY | 44 | ≥40 ✅ 已达标 |
| BusinessDomain 连通率 | 0% | ≥50% |
| 核心链路 Controller→Table | 0 | ≥5 |
| 扫描耗时 | 774s | ≤800s ✅ 已达标 |
